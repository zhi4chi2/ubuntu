在 Windows 中，每个存储设备都有一个独立的文件系统树。而在 Linux 中，只有一个文件系统树，存储设备挂载到文件系统树的不同位置。


每个用户都有一个主目录，对于普通用户，这是唯一可写的地方。


文件名以 "." 开始的文件是隐藏文件，所谓隐藏文件即用 "ls" 命令不会显示，用 "ls -a" 才会显示。


文件名和命令都区分大小写。


创建文件名时仅可使用 ".", "-", "_" 不可以使用其它特殊字符。但是已有的文件名可能包含空格和其它标点符号，最好不要在文件名中使用空格，这会导致很多问题。
